<ContentDialog
    x:Class="FileManager.Controlls.ContentDialogControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileManager.Controlls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vml="using:FileManager.VMLocator" 
    vml:ViewModelLocator.AutoWireViewModel="True"
    Title="{Binding Title}"
    PrimaryButtonText="{Binding PrimaryButtonContent}"
    SecondaryButtonText="{Binding SecondaryButtonContent}">

    <Grid>
        <TextBox Text="{Binding InputText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="{Binding PlaceHolder}" FontSize="20" TextAlignment="Left" />
    </Grid>
</ContentDialog>
